[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "xauusd-scalper"
version = "0.1.0"
description = "Strict XAUUSD 5-minute scalping setup evaluator"
requires-python = ">=3.11"
readme = "README.md"

[tool.setuptools.packages.find]
where = ["src"]
